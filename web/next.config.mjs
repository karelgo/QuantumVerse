// Plain JS (not .ts) so `next start` doesn't need the typescript package
// at runtime — the production image prunes devDependencies.

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
